<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://sudhirpol522.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://sudhirpol522.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-08-30T23:47:46+00:00</updated><id>https://sudhirpol522.github.io/feed.xml</id><title type="html">Sudhir Pol</title><subtitle>Portfolio of Sudhir Pol — machine learning engineer focused on LLM inference, model serving, evaluation, and production machine learning systems. </subtitle><entry><title type="html">Medusa From the Tensors Up: Self-Speculative Decoding Without a Draft Model</title><link href="https://sudhirpol522.github.io/blog/medusa-multi-token-prediction/" rel="alternate" type="text/html" title="Medusa From the Tensors Up: Self-Speculative Decoding Without a Draft Model"/><published>2026-08-30T00:00:00+00:00</published><updated>2026-08-30T00:00:00+00:00</updated><id>https://sudhirpol522.github.io/blog/medusa-multi-token-prediction</id><content type="html" xml:base="https://sudhirpol522.github.io/blog/medusa-multi-token-prediction/"><![CDATA[<p>In my <a href="/blog/speculative-decoding-math/">previous post</a>, speculative decoding used a separate draft model. The draft proposed a short continuation, and the target model verified it in parallel. The mathematics was elegant, but the system now had to train, load, and serve two models.</p> <p>Medusa asks a wonderfully practical question:</p> <blockquote> <p><strong>What if the target model could speculate for itself?</strong></p> </blockquote> <p>Instead of loading a separate draft model, Medusa attaches a few lightweight prediction heads to the target model’s final hidden state. The normal language-model head predicts the next token, while the extra heads predict tokens farther into the future.</p> <p>That one sentence hides most of the interesting details. What exactly is a “head”? Which parameters are trained? How are the labels shifted? Why are later heads less accurate? How can four heads create a verification input with roughly 250 tokens? And why is the recursive MTP implementation in vLLM Speculators not the same architecture as Medusa?</p> <p>This post answers those questions from the tensors upward.</p> <h2 id="part-1-one-hidden-state-not-one-logits-state">Part 1: One Hidden State, Not One Logits State</h2> <p>Consider the tokenized sentence:</p> <blockquote> <p>I like cats today .</p> </blockquote> <p>Let the current position be the token <code class="language-plaintext highlighter-rouge">I</code>, at index \(t\). After the Transformer processes the context through position \(t\), it produces a final hidden state:</p> \[h_t \in \mathbb{R}^{d},\] <p>where \(d\) is the model’s hidden dimension. This is a hidden representation, not a vector of vocabulary logits.</p> <p>The original LM head maps that representation to one score per vocabulary item:</p> \[\ell_t^{(0)} = W_{\mathrm{LM}}h_t \in \mathbb{R}^{V},\] <p>and predicts the immediate next token:</p> \[h_t \xrightarrow{\text{LM head}} x_{t+1}.\] <p>With three auxiliary Medusa heads, the same \(h_t\) produces three additional vocabulary distributions:</p> \[\begin{aligned} h_t \xrightarrow{\text{Medusa head 1}} &amp;\ x_{t+2} &amp;&amp; \text{(cats)}, \\ h_t \xrightarrow{\text{Medusa head 2}} &amp;\ x_{t+3} &amp;&amp; \text{(today)}, \\ h_t \xrightarrow{\text{Medusa head 3}} &amp;\ x_{t+4} &amp;&amp; \text{(.)}. \end{aligned}\] <p><img src="/assets/img/medusa-vs-recursive-mtp.svg" alt="Medusa's independent heads compared with recursive MTP"/></p> <p>The distinction is important:</p> <ul> <li>There is one shared backbone state, \(h_t\).</li> <li>There are multiple logit tensors, one from each prediction head.</li> <li>Classic Medusa heads do not receive the predictions or activations of earlier Medusa heads.</li> </ul> <p>That last property makes the heads cheap and parallel, but it also explains why prediction quality generally deteriorates at more distant positions.</p> <h2 id="part-2-what-is-inside-a-medusa-head">Part 2: What Is Inside a Medusa Head?</h2> <p>The Medusa paper defines each auxiliary head as a residual feed-forward transformation followed by a vocabulary projection:</p> \[p_t^{(k)} = \operatorname{softmax}\left( W_2^{(k)} \left[ \operatorname{SiLU}\left(W_1^{(k)}h_t\right)+h_t \right] \right).\] <p>For head \(k\):</p> <ul> <li>\(W_1^{(k)} \in \mathbb{R}^{d \times d}\) is its residual transformation.</li> <li>\(W_2^{(k)} \in \mathbb{R}^{V \times d}\) projects into the vocabulary.</li> <li>\(p_t^{(k)} \in \mathbb{R}^{V}\) is a complete probability distribution over the vocabulary.</li> </ul> <p>The paper initializes \(W_1^{(k)}\) to zero and copies the original LM-head weights into \(W_2^{(k)}\). At initialization, the residual branch contributes nothing, so every new head begins with the base model’s next-token mapping rather than arbitrary logits.</p> <p>A minimal PyTorch version looks like this:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">MedusaHead</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">hidden_size</span><span class="p">,</span> <span class="n">vocab_size</span><span class="p">,</span> <span class="n">base_lm_head</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>

        <span class="n">self</span><span class="p">.</span><span class="n">residual</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span>
            <span class="n">hidden_size</span><span class="p">,</span>
            <span class="n">hidden_size</span><span class="p">,</span>
            <span class="n">bias</span><span class="o">=</span><span class="bp">False</span><span class="p">,</span>
        <span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">vocab_projection</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span>
            <span class="n">hidden_size</span><span class="p">,</span>
            <span class="n">vocab_size</span><span class="p">,</span>
            <span class="n">bias</span><span class="o">=</span><span class="bp">False</span><span class="p">,</span>
        <span class="p">)</span>

        <span class="n">nn</span><span class="p">.</span><span class="n">init</span><span class="p">.</span><span class="nf">zeros_</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">residual</span><span class="p">.</span><span class="n">weight</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">vocab_projection</span><span class="p">.</span><span class="n">weight</span><span class="p">.</span><span class="n">data</span><span class="p">.</span><span class="nf">copy_</span><span class="p">(</span>
            <span class="n">base_lm_head</span><span class="p">.</span><span class="n">weight</span><span class="p">.</span><span class="n">data</span>
        <span class="p">)</span>

    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">hidden_states</span><span class="p">):</span>
        <span class="n">transformed</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">silu</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="nf">residual</span><span class="p">(</span><span class="n">hidden_states</span><span class="p">))</span>
        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">vocab_projection</span><span class="p">(</span><span class="n">hidden_states</span> <span class="o">+</span> <span class="n">transformed</span><span class="p">)</span>
</code></pre></div></div> <p>If we configure three Medusa heads, we have three independent instances of this module plus the original LM head. The auxiliary projections have separate trainable parameters even though they share the same initialization.</p> <h2 id="part-3-medusa-1-freezes-the-backbone">Part 3: Medusa-1 Freezes the Backbone</h2> <p>Medusa-1 is a post-training recipe. We begin with an already trained target model and freeze it:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="n">parameter</span> <span class="ow">in</span> <span class="n">backbone</span><span class="p">.</span><span class="nf">parameters</span><span class="p">():</span>
    <span class="n">parameter</span><span class="p">.</span><span class="n">requires_grad</span> <span class="o">=</span> <span class="bp">False</span>
</code></pre></div></div> <p>Only the newly attached Medusa heads receive optimizer updates:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">optimizer</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">optim</span><span class="p">.</span><span class="nc">AdamW</span><span class="p">(</span>
    <span class="n">medusa_heads</span><span class="p">.</span><span class="nf">parameters</span><span class="p">(),</span>
    <span class="n">lr</span><span class="o">=</span><span class="mf">1e-3</span><span class="p">,</span>
<span class="p">)</span>
</code></pre></div></div> <p>The token embeddings, Transformer blocks, normalization layers, and original LM head are all part of the frozen backbone. We still run the backbone to obtain hidden states, but gradients do not update it.</p> <p>This gives Medusa-1 three useful operational properties:</p> <ul> <li>No separate draft model must be trained and hosted.</li> <li>Training cannot alter the target model’s original next-token behavior.</li> <li>The frozen backbone can be quantized while the auxiliary heads are trained, reducing memory use.</li> </ul> <p>Medusa-2 changes the recipe by jointly updating the backbone and auxiliary heads with a carefully balanced objective. Here, I will stay with the simpler Medusa-1 setting.</p> <h2 id="part-4-the-loss-is-shifted-cross-entropy">Part 4: The Loss Is Shifted Cross-Entropy</h2> <p>Suppose a batch contains this sequence of token IDs:</p> \[[x_0,x_1,x_2,x_3,x_4] = [\text{I},\text{like},\text{cats},\text{today},\text{.}].\] <p>At every input position, each head is trained against a different future offset:</p> <table> <thead> <tr> <th>Prediction source</th> <th>Logits at position \(t\) target</th> <th style="text-align: right">Shift</th> </tr> </thead> <tbody> <tr> <td>Original LM head</td> <td>\(x_{t+1}\)</td> <td style="text-align: right">1</td> </tr> <tr> <td>Medusa head 0</td> <td>\(x_{t+2}\)</td> <td style="text-align: right">2</td> </tr> <tr> <td>Medusa head 1</td> <td>\(x_{t+3}\)</td> <td style="text-align: right">3</td> </tr> <tr> <td>Medusa head 2</td> <td>\(x_{t+4}\)</td> <td style="text-align: right">4</td> </tr> </tbody> </table> <p>For a Medusa head indexed from zero in Python:</p> \[\operatorname{shift}_k = k+2.\] <p>Its cross-entropy loss is:</p> \[\mathcal{L}_k = -\frac{1}{N_k} \sum_t \log p_t^{(k)}\left(x_{t+k+2}\right),\] <p>where \(N_k\) is the number of valid, non-padding targets for that head.</p> <p>The slicing operation is surprisingly small:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">shifted_cross_entropy</span><span class="p">(</span><span class="n">logits</span><span class="p">,</span> <span class="n">labels</span><span class="p">,</span> <span class="n">shift</span><span class="p">,</span> <span class="n">ignore_index</span><span class="o">=-</span><span class="mi">100</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    logits[:, t] predicts labels[:, t + shift].
    </span><span class="sh">"""</span>
    <span class="k">if</span> <span class="n">shift</span> <span class="o">&lt;=</span> <span class="mi">0</span><span class="p">:</span>
        <span class="k">raise</span> <span class="nc">ValueError</span><span class="p">(</span><span class="sh">"</span><span class="s">shift must be positive</span><span class="sh">"</span><span class="p">)</span>
    <span class="k">if</span> <span class="n">logits</span><span class="p">.</span><span class="nf">size</span><span class="p">(</span><span class="mi">1</span><span class="p">)</span> <span class="o">&lt;=</span> <span class="n">shift</span><span class="p">:</span>
        <span class="k">raise</span> <span class="nc">ValueError</span><span class="p">(</span>
            <span class="sa">f</span><span class="sh">"</span><span class="s">sequence length </span><span class="si">{</span><span class="n">logits</span><span class="p">.</span><span class="nf">size</span><span class="p">(</span><span class="mi">1</span><span class="p">)</span><span class="si">}</span><span class="s"> must exceed shift </span><span class="si">{</span><span class="n">shift</span><span class="si">}</span><span class="sh">"</span>
        <span class="p">)</span>

    <span class="n">shifted_logits</span> <span class="o">=</span> <span class="n">logits</span><span class="p">[:,</span> <span class="p">:</span><span class="o">-</span><span class="n">shift</span><span class="p">,</span> <span class="p">:].</span><span class="nf">contiguous</span><span class="p">()</span>
    <span class="n">shifted_labels</span> <span class="o">=</span> <span class="n">labels</span><span class="p">[:,</span> <span class="n">shift</span><span class="p">:].</span><span class="nf">contiguous</span><span class="p">()</span>

    <span class="k">return</span> <span class="n">F</span><span class="p">.</span><span class="nf">cross_entropy</span><span class="p">(</span>
        <span class="n">shifted_logits</span><span class="p">.</span><span class="nf">reshape</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">shifted_logits</span><span class="p">.</span><span class="nf">size</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)).</span><span class="nf">float</span><span class="p">(),</span>
        <span class="n">shifted_labels</span><span class="p">.</span><span class="nf">reshape</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">),</span>
        <span class="n">ignore_index</span><span class="o">=</span><span class="n">ignore_index</span><span class="p">,</span>
    <span class="p">)</span>
</code></pre></div></div> <p>For a sequence of length five, the valid pairs shrink as the shift grows:</p> <table> <thead> <tr> <th>Head</th> <th>Valid logits positions</th> <th>Target positions</th> <th style="text-align: right">Training pairs</th> </tr> </thead> <tbody> <tr> <td>Base, shift 1</td> <td>\([0,1,2,3]\)</td> <td>\([1,2,3,4]\)</td> <td style="text-align: right">4</td> </tr> <tr> <td>Medusa 0, shift 2</td> <td>\([0,1,2]\)</td> <td>\([2,3,4]\)</td> <td style="text-align: right">3</td> </tr> <tr> <td>Medusa 1, shift 3</td> <td>\([0,1]\)</td> <td>\([3,4]\)</td> <td style="text-align: right">2</td> </tr> <tr> <td>Medusa 2, shift 4</td> <td>\([0]\)</td> <td>\([4]\)</td> <td style="text-align: right">1</td> </tr> </tbody> </table> <p>This reveals an easy-to-miss engineering detail: later heads receive fewer supervised positions near sequence boundaries. A sequence of length three can train shifts 1 and 2, but a shift of 3 produces empty tensors unless the implementation guards against it.</p> <h3 id="one-numerical-cross-entropy-calculation">One Numerical Cross-Entropy Calculation</h3> <p>Take a toy vocabulary with five tokens:</p> \[[\text{I},\text{like},\text{cats},\text{dogs},\text{today}].\] <p>At position <code class="language-plaintext highlighter-rouge">I</code>, Medusa head 0 is trained with shift 2, so its target is <code class="language-plaintext highlighter-rouge">cats</code>. Suppose it emits:</p> \[\ell=[0,0,1.5,0,0].\] <p>The probability assigned to <code class="language-plaintext highlighter-rouge">cats</code> is:</p> \[\frac{e^{1.5}}{e^{1.5}+4e^0} = \frac{4.4817}{8.4817} \approx 0.5284.\] <p>Therefore, this position contributes:</p> \[-\log(0.5284) \approx 0.6379.\] <p>PyTorch performs this calculation for every valid batch-position pair and averages the negative log-probabilities.</p> <h3 id="weighting-the-heads">Weighting the Heads</h3> <p>Later positions are harder because they are more uncertain. The Medusa paper therefore proposes downweighting later-head losses:</p> \[\mathcal{L}_{\text{Medusa-1}} = \sum_{k=1}^{K}\lambda_k\mathcal{L}_k, \qquad \lambda_k \approx 0.8^k.\] <p>In Python:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">medusa1_loss</span><span class="p">(</span><span class="n">medusa_logits</span><span class="p">,</span> <span class="n">labels</span><span class="p">,</span> <span class="n">decay</span><span class="o">=</span><span class="mf">0.8</span><span class="p">):</span>
    <span class="n">losses</span> <span class="o">=</span> <span class="p">[]</span>

    <span class="k">for</span> <span class="n">head_index</span><span class="p">,</span> <span class="n">logits</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">medusa_logits</span><span class="p">):</span>
        <span class="n">loss</span> <span class="o">=</span> <span class="nf">shifted_cross_entropy</span><span class="p">(</span>
            <span class="n">logits</span><span class="p">,</span>
            <span class="n">labels</span><span class="p">,</span>
            <span class="n">shift</span><span class="o">=</span><span class="n">head_index</span> <span class="o">+</span> <span class="mi">2</span><span class="p">,</span>
        <span class="p">)</span>
        <span class="n">losses</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">loss</span><span class="p">)</span>

    <span class="n">total</span> <span class="o">=</span> <span class="nf">sum</span><span class="p">(</span>
        <span class="n">decay</span> <span class="o">**</span> <span class="p">(</span><span class="n">head_index</span> <span class="o">+</span> <span class="mi">1</span><span class="p">)</span> <span class="o">*</span> <span class="n">loss</span>
        <span class="k">for</span> <span class="n">head_index</span><span class="p">,</span> <span class="n">loss</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">losses</span><span class="p">)</span>
    <span class="p">)</span>

    <span class="k">return</span> <span class="n">total</span><span class="p">,</span> <span class="n">losses</span>
</code></pre></div></div> <p>Because the backbone is frozen, Medusa-1 does not need the original next-token loss to protect the base model. That loss can still be monitored as an evaluation metric, but it contributes no useful gradient to frozen parameters.</p> <h2 id="part-5-why-later-heads-usually-have-higher-loss">Part 5: Why Later Heads Usually Have Higher Loss</h2> <p>A tempting intuition is that a later head has “more context” because it predicts a later token. In classic Medusa, the opposite is true: every auxiliary head receives the same context representation, \(h_t\).</p> <p>Head 3 must predict farther into the future without observing the tokens that heads 1 and 2 are trying to predict:</p> \[\begin{aligned} p(x_{t+2}\mid x_{\le t}) &amp;\quad \text{Head 1}, \\ p(x_{t+3}\mid x_{\le t}) &amp;\quad \text{Head 2}, \\ p(x_{t+4}\mid x_{\le t}) &amp;\quad \text{Head 3}. \end{aligned}\] <p>The later distributions average over more possible unseen continuations. Their entropy tends to be higher, so the typical aggregate ordering is:</p> \[\mathcal{L}_{\text{base}} &lt; \mathcal{L}_{\text{head 1}} &lt; \mathcal{L}_{\text{head 2}} &lt; \mathcal{L}_{\text{head 3}}.\] <p>This is an empirical tendency, not a guarantee for every minibatch. It motivates both decaying loss weights and architectures that explicitly feed earlier future tokens into later predictions.</p> <h2 id="part-6-mtp-is-a-family-not-a-single-architecture">Part 6: MTP Is a Family, Not a Single Architecture</h2> <p>“Multi-token prediction” describes a task: predict several future tokens from one observed prefix. It does not uniquely specify the network architecture.</p> <h3 id="original-parallel-mtp">Original Parallel MTP</h3> <p>The MTP work by Gloeckle et al. trains multi-token prediction during pretraining. A shared Transformer trunk produces a representation \(z_t\), and independent head-specific Transformer layers predict several future positions:</p> \[p_i = \operatorname{softmax}\left( f_u\left(f_{h_i}(z_t)\right) \right).\] <p>The head transformations \(f_{h_i}\) are independent, but the expensive unembedding matrix \(f_u\) is shared. This differs from the separate auxiliary vocabulary projections described in the Medusa paper.</p> <h3 id="recursive-mtp-in-vllm-speculators">Recursive MTP in vLLM Speculators</h3> <p>The current vLLM Speculators MTP implementation fine-tunes a model’s native MTP layer in a recursive, FastMTP-style design. At each speculative step, the layer combines:</p> <ol> <li>The previous hidden representation.</li> <li>The embedding of the preceding future token.</li> </ol> <p>For the sentence <code class="language-plaintext highlighter-rouge">I like cats today</code>, training proceeds conceptually as:</p> \[z_0 = \operatorname{MTP}\left(h_t,E(\text{like})\right) \quad\longrightarrow\quad \text{predict cats},\] \[z_1 = \operatorname{MTP}\left(z_0,E(\text{cats})\right) \quad\longrightarrow\quad \text{predict today}.\] <p>The MTP layer is reused recursively. During training, the token embeddings come from ground-truth tokens through teacher forcing. During inference, they come from earlier predicted tokens.</p> <p>With three speculative steps and the documented default decay parameter \(\beta=0.6\), the normalized step weights are approximately:</p> \[[\alpha_0,\alpha_1,\alpha_2]=[0.51,0.31,0.18],\] <p>and the total loss is:</p> \[0.51\mathcal{L}_0 +0.31\mathcal{L}_1 +0.18\mathcal{L}_2.\] <p>The verifier, embedding table, and LM head remain frozen and shared; only the native MTP layer is fine-tuned.</p> <table> <thead> <tr> <th>Property</th> <th>Medusa-1</th> <th>Original parallel MTP</th> <th>vLLM recursive MTP</th> </tr> </thead> <tbody> <tr> <td>Training stage</td> <td>Post-training</td> <td>Pretraining</td> <td>Fine-tuning native MTP weights</td> </tr> <tr> <td>Future-position modules</td> <td>Independent residual heads</td> <td>Independent Transformer heads</td> <td>One recursively reused MTP layer</td> </tr> <tr> <td>Earlier future tokens visible</td> <td>No</td> <td>No</td> <td>Yes</td> </tr> <tr> <td>Token embeddings used by predictor</td> <td>No</td> <td>No</td> <td>Yes</td> </tr> <tr> <td>Vocabulary projection</td> <td>Separate auxiliary projections</td> <td>Shared unembedding</td> <td>Shared frozen LM head</td> </tr> <tr> <td>Backbone updated</td> <td>No</td> <td>Yes</td> <td>No</td> </tr> </tbody> </table> <p>Calling all three approaches “MTP” is reasonable at the task level. Calling their implementations identical is not.</p> <h2 id="part-7-training-heads-and-building-a-tree-are-different-operations">Part 7: Training Heads and Building a Tree Are Different Operations</h2> <p>Training uses one ground-truth label per future position. There is no Cartesian product, candidate tree, or tree-attention mask in the Medusa-1 training loss.</p> <p>The tree appears only during inference.</p> <p>Each trained Medusa head emits \(V\) logits. We retain only its highest-ranked candidates. Define:</p> \[s_i = \text{number of top candidate tokens retained from head }i.\] <p>The values \(s_i\) are inference hyperparameters. They are not learned weights, and they are not the number of Medusa heads.</p> <h3 id="a-two-head-example">A Two-Head Example</h3> <p>Suppose the base LM head selects <code class="language-plaintext highlighter-rouge">like</code>. Then:</p> <ul> <li>Medusa head 1 retains <code class="language-plaintext highlighter-rouge">cats</code> and <code class="language-plaintext highlighter-rouge">dogs</code>, so \(s_1=2\).</li> <li>Medusa head 2 retains <code class="language-plaintext highlighter-rouge">today</code> and <code class="language-plaintext highlighter-rouge">too</code>, so \(s_2=2\).</li> </ul> <p><img src="/assets/img/medusa-candidate-tree.svg" alt="A two-level Medusa candidate tree with two choices per head"/></p> <p>Because classic Medusa head 2 did not condition on head 1, its same two token labels are attached below both first-level branches. They become distinct verification nodes because <code class="language-plaintext highlighter-rouge">today</code> after <code class="language-plaintext highlighter-rouge">like cats</code> has a different context from <code class="language-plaintext highlighter-rouge">today</code> after <code class="language-plaintext highlighter-rouge">like dogs</code>.</p> <p>The four complete candidate continuations are:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>like cats today
like cats too
like dogs today
like dogs too
</code></pre></div></div> <p>The number of complete candidates, or leaves, is:</p> \[s_1s_2 = 2 \times 2 = 4.\] <p>But the verification tree contains both intermediate prefix nodes and leaves:</p> \[q=s_1+s_1s_2=2+4=6.\] <p>Tree attention packs those six candidate-token positions into one target-model verification pass. Its mask ensures that each node can see only the prompt and the ancestors on its own branch.</p> <h3 id="where-does-an-additional-length-near-250-come-from">Where Does an Additional Length Near 250 Come From?</h3> <p>With \(K\) Medusa heads, a regular Cartesian tree has:</p> \[q = s_1 +s_1s_2 +s_1s_2s_3 +\cdots +\prod_{i=1}^{K}s_i.\] <p>Suppose four heads retain:</p> \[(s_1,s_2,s_3,s_4)=(5,5,3,2).\] <p>Then the tree contains:</p> \[\begin{aligned} \text{depth 1:}&amp;\quad 5, \\ \text{depth 2:}&amp;\quad 5\times5=25, \\ \text{depth 3:}&amp;\quad 5\times5\times3=75, \\ \text{depth 4:}&amp;\quad 5\times5\times3\times2=150. \end{aligned}\] <p>Therefore:</p> \[q=5+25+75+150=255.\] <p>That is how four learned heads can create an additional verification length near 250. The model does not contain 250 heads. It contains four heads whose retained alternatives create 255 contextual token nodes.</p> <p>In practice, the tree need not be a full Cartesian product. Low-value branches can be pruned, producing an irregular tree that spends the verification budget on more promising paths.</p> <h2 id="part-8-verification-is-where-the-speedup-comes-from">Part 8: Verification Is Where the Speedup Comes From</h2> <p>The Medusa heads are cheap, but their outputs are only proposals. The target model still decides which continuation is valid.</p> <p>Tree attention makes that verification efficient:</p> <ol> <li>Flatten the candidate tree into token positions.</li> <li>Assign position IDs according to tree depth.</li> <li>Construct an attention mask so each node sees only its branch ancestors.</li> <li>Run the target model over all tree nodes in parallel.</li> <li>Accept the longest valid prefix under the selected acceptance rule.</li> </ol> <p>Under greedy decoding, matching the target’s greedy choices preserves the original output exactly. Rejection sampling can preserve the target distribution for sampling. Medusa’s optional typical-acceptance rule can accept more tokens, but it relaxes exact distribution matching in exchange for additional speed while aiming to preserve generation quality.</p> <p>Increasing \(s_i\) makes it more likely that the target model finds a useful branch, but it also increases \(q\) and therefore verification cost. This creates an interior optimum like the one in standard speculative decoding: more speculation helps until verification overhead dominates.</p> <p>The Medusa paper’s hardware model illustrates this trade-off clearly. In one simulated setting, speedup improves as the candidate-token count grows, peaks around 64 candidates, and then declines as extra verification work overwhelms the benefit.</p> <h2 id="part-9-what-i-would-measure-as-an-mle">Part 9: What I Would Measure as an MLE</h2> <p>A lower training loss is necessary, but it is not the final objective. An inference optimization should be evaluated end to end.</p> <table> <thead> <tr> <th>Metric</th> <th>Why it matters</th> </tr> </thead> <tbody> <tr> <td>Per-head cross-entropy</td> <td>Shows how uncertainty grows with prediction distance</td> </tr> <tr> <td>Per-head top-1 and top-\(k\) accuracy</td> <td>Helps choose useful branching factors</td> </tr> <tr> <td>Mean accepted tokens per verification</td> <td>Measures actual decoding-step compression</td> </tr> <tr> <td>Candidate-tree node count \(q\)</td> <td>Captures the verification workload</td> </tr> <tr> <td>Target forward calls per generated token</td> <td>Measures saved sequential work independently of hardware</td> </tr> <tr> <td>Time to first token</td> <td>Confirms that the optimization targets decode rather than prompt prefill</td> </tr> <tr> <td>Inter-token latency</td> <td>Captures the user-visible low-batch latency improvement</td> </tr> <tr> <td>Throughput under concurrency</td> <td>Reveals when ordinary batching already saturates the GPU</td> </tr> <tr> <td>Greedy exact match or sampling-distribution tests</td> <td>Validates the selected acceptance rule</td> </tr> </tbody> </table> <p>Wall-clock speedup is workload-dependent. Medusa is most compelling when autoregressive decoding is memory-bandwidth-bound and the serving batch is small enough that the GPU has spare arithmetic capacity. At high concurrency, ordinary batching may already use that capacity, making a larger verification tree less attractive.</p> <h2 id="the-mental-model-to-keep">The Mental Model to Keep</h2> <p>Medusa-1 is easiest to understand as three separate stages:</p> <ol> <li><strong>Training:</strong> Freeze the target model and train independent residual heads with shifted future-token cross-entropy.</li> <li><strong>Candidate construction:</strong> At inference, keep selected top tokens from each head and combine them into a tree.</li> <li><strong>Verification:</strong> Run the target model once over that tree with a branch-aware attention mask and accept the longest valid prefix.</li> </ol> <p>Recursive MTP keeps the same high-level goal, but changes the predictor. Instead of independent parallel heads over one hidden state, it reuses an MTP layer and feeds previous speculative activations and token embeddings forward.</p> <p>That distinction is more than terminology. It determines parameter sharing, exposure bias, loss alignment, serving integration, and how quickly prediction quality degrades with depth.</p> <p>The broader engineering lesson is simple: “predict multiple tokens” describes an objective, not an implementation. The speedup lives in the interaction between the training architecture, proposal structure, acceptance rate, and hardware cost of verification.</p> <hr/> <h2 id="references">References</h2> <ul> <li>Cai et al., <a href="https://arxiv.org/abs/2401.10774">Medusa: Simple LLM Inference Acceleration Framework with Multiple Decoding Heads</a></li> <li>FasterDecoding, <a href="https://github.com/FasterDecoding/Medusa">official Medusa implementation</a></li> <li>Gloeckle et al., <a href="https://arxiv.org/abs/2404.19737">Better &amp; Faster Large Language Models via Multi-token Prediction</a></li> <li>vLLM Project, <a href="https://docs.vllm.ai/projects/speculators/en/stable/user_guide/algorithms/mtp/">Speculators MTP documentation</a></li> </ul>]]></content><author><name></name></author><category term="technical-writing"/><category term="llm-inference"/><category term="speculative-decoding"/><category term="medusa"/><category term="multi-token-prediction"/><summary type="html"><![CDATA[A tensor-level guide to Medusa heads, shifted cross-entropy, candidate trees, verification, and the difference between Medusa and recursive MTP.]]></summary></entry><entry><title type="html">Unpacking Speculative Decoding: The Math Behind the Speedup</title><link href="https://sudhirpol522.github.io/blog/speculative-decoding-math/" rel="alternate" type="text/html" title="Unpacking Speculative Decoding: The Math Behind the Speedup"/><published>2026-06-22T00:00:00+00:00</published><updated>2026-06-22T00:00:00+00:00</updated><id>https://sudhirpol522.github.io/blog/speculative-decoding-math</id><content type="html" xml:base="https://sudhirpol522.github.io/blog/speculative-decoding-math/"><![CDATA[<p>If you’ve been following my recent posts on optimizing LLM inference, you know that making models faster is an ongoing battle. We’ve discussed shrinking models using quantization techniques like AWQ and GPTQ to reduce memory bandwidth. But what if you want to speed up inference <em>without</em> changing the target model’s weights at all?</p> <p>In my previous post, <a href="https://medium.com/@sudhirpol522/speculative-decoding-the-clever-trick-making-llms-2-faster-69a2adee98a7">Speculative Decoding: The Clever Trick Making LLMs 2x Faster</a>, we looked at the high-level intuition. Today, we are opening the hood. We are going to derive the exact mathematical objective function that governs speculative decoding, prove why it guarantees zero degradation in quality, and reveal a brutal reality about token probabilities: <strong>why failing early is devastating.</strong></p> <h2 id="the-setup">The Setup</h2> <p>Let’s quickly define our variables. We have two autoregressive models:</p> <ul> <li><strong>Target Model \(p(x)\)</strong>: Our large, expensive model. We must match its output distribution exactly.</li> <li><strong>Draft Model \(q(x)\)</strong>: A small, cheap model that proposes tokens quickly.</li> </ul> <p>In every iteration, the Draft model autoregressively proposes \(\gamma\) tokens (the lookahead). The Target model then verifies all of them in one parallel forward pass.</p> <hr/> <h2 id="part-1-the-single-token-acceptance-probability-beta">Part 1: The Single-Token Acceptance Probability (\(\beta\))</h2> <p>This is the heart of speculative decoding. How do we decide if we keep a drafted token? The rule is:</p> <ol> <li>Draft samples a token \(x \sim q(x)\).</li> <li>Accept \(x\) with probability \(\min\left(1, \frac{p(x)}{q(x)}\right)\).</li> </ol> <p>To find the expected acceptance rate (\(\beta\)) across all possible tokens, we multiply the probability that the draft picks a token by the probability that we accept it, and sum this over the entire vocabulary:</p> \[\beta = \sum_x q(x) \cdot \min\left(1, \frac{p(x)}{q(x)}\right)\] <p>Push \(q(x)\) inside the \(\min\) function, and it simplifies beautifully:</p> \[\beta = \sum_x \min(q(x), p(x))\] <p><strong>In simple English:</strong> The acceptance probability is exactly equal to the “overlap mass” between the Target and Draft distributions. If they agree perfectly everywhere, \(\beta = 1\).</p> <h3 id="the-total-variation-distance-identity">The Total Variation Distance Identity</h3> <p>We can rewrite this using a standard mathematical identity: \(\min(a,b) = \frac{a+b-\lvert a-b \rvert}{2}\).</p> \[\sum_x \min(p,q) = \frac{1}{2}\sum_x \big(p(x) + q(x) - \lvert p(x) - q(x) \rvert\big)\] <p>Because \(p\) and \(q\) are valid probability distributions, they both sum to \(1\).</p> \[= \frac{1}{2}\big(1 + 1 - \sum_x \lvert p(x) - q(x) \rvert\big)\] <p>By definition, Total Variation (TV) distance is \(D_{TV}(p,q) = \frac{1}{2}\sum_x \lvert p(x) - q(x) \rvert\). Substituting this in gives us our final, elegant identity:</p> \[\beta = 1 - D_{TV}(p,q)\] <p>A better-aligned draft model has a smaller TV distance to the target, which directly raises the acceptance rate.</p> <hr/> <h2 id="part-2-a-concrete-example-vocabulary-of-5">Part 2: A Concrete Example (Vocabulary of 5)</h2> <p>Let’s look at real numbers to see this identity hold true. Imagine a vocabulary of just 5 words. The Draft model proposes a sequence of 3 tokens. Let’s calculate the acceptance probability (\(\beta_1\)) for the very first drafted token.</p> <table> <thead> <tr> <th>Token 1</th> <th>Word A</th> <th>Word B</th> <th>Word C</th> <th>Word D</th> <th>Word E</th> </tr> </thead> <tbody> <tr> <td><strong>Target \(p\)</strong></td> <td>0.50</td> <td>0.20</td> <td>0.15</td> <td>0.10</td> <td>0.05</td> </tr> <tr> <td><strong>Draft \(q\)</strong></td> <td>0.38</td> <td>0.25</td> <td>0.20</td> <td>0.10</td> <td>0.07</td> </tr> <tr> <td><strong>\(\min(p,q)\)</strong></td> <td>0.38</td> <td>0.20</td> <td>0.15</td> <td>0.10</td> <td>0.05</td> </tr> <tr> <td><strong>\(\lvert p-q \rvert\)</strong></td> <td>0.12</td> <td>0.05</td> <td>0.05</td> <td>0.00</td> <td>0.02</td> </tr> </tbody> </table> <p>Let’s compute \(\beta_1\) using both methods we just derived:</p> <p><strong>Method 1: Overlap Mass</strong></p> \[\beta_1 = \sum \min(p,q) = 0.38 + 0.20 + 0.15 + 0.10 + 0.05 = \mathbf{0.88}\] <p><strong>Method 2: Total Variation Distance</strong> The sum of the absolute differences \(\sum \lvert p-q \rvert = 0.24\).</p> \[D_{TV} = \frac{0.24}{2} = 0.12\] \[\beta_1 = 1 - 0.12 = \mathbf{0.88}\] <p>Both methods perfectly yield <strong>0.88</strong>.</p> <p>Now, assume we calculate the next two tokens in the drafted sequence. Token 2 happens to be very well-aligned, but Token 3 is poorly aligned:</p> <ul> <li><strong>Token 1:</strong> \(\beta_1 = 0.88\)</li> <li><strong>Token 2:</strong> \(\beta_2 = 0.96\)</li> <li><strong>Token 3:</strong> \(\beta_3 = 0.65\)</li> </ul> <hr/> <h2 id="part-3-expected-tokens-and-the-domino-effect">Part 3: Expected Tokens and The Domino Effect</h2> <p>Because speculative decoding strictly evaluates left-to-right, a token only counts if <strong>every token before it was also accepted.</strong> Using our actual per-position acceptance probabilities, we can compute exactly how many drafted tokens we expect to keep (\(\mathbb{E}[n]\)) from our 3-token sequence:</p> \[\mathbb{E}[n] = \beta_1 + (\beta_1 \cdot \beta_2) + (\beta_1 \cdot \beta_2 \cdot \beta_3)\] \[\mathbb{E}[n] = 0.88 + (0.88 \times 0.96) + (0.88 \times 0.96 \times 0.65)\] \[\mathbb{E}[n] = 0.88 + 0.8448 + 0.54912 = \mathbf{2.274 \text{ tokens}}\] <h3 id="the-1-guaranteed-bonus-token">The +1 Guaranteed Bonus Token</h3> <p>Here is a subtlety often missed: Every iteration yields one extra token beyond the accepted prefix.</p> <ul> <li>If a rejection happens, the Target model resamples a valid token from the residual distribution to replace it.</li> <li>If all tokens are accepted, the Target’s parallel forward pass naturally yields the logit for the <em>next</em> unseen token.</li> </ul> <p>Either way, you always get \(+1\) token.</p> \[\mathbb{E}[X] = \mathbb{E}[n] + 1 \approx \mathbf{3.27 \text{ real tokens per iteration}}\] <h3 id="why-failing-early-is-devastating-order-matters">Why Failing Early is Devastating (Order Matters)</h3> <p>Notice that our weak draft (\(\beta_3 = 0.65\)) sat at the very end of the sequence. What if that weak token had been proposed first?</p> <p>If \(\beta_1 = 0.65\), \(\beta_2 = 0.96\), and \(\beta_3 = 0.88\), our expected draft tokens would drop dramatically:</p> \[\mathbb{E}[n] = 0.65 + (0.65 \times 0.96) + (0.65 \times 0.96 \times 0.88) = \mathbf{1.82 \text{ tokens}}\] <p>A wrong early token acts as a bottleneck, instantly rendering the rest of the drafted block as wasted compute. This is exactly why, when tuning draft models, accuracy on the <em>first</em> few positions matters significantly more than the end of the sequence.</p> <p><em>(Note: In literature, you will often see this mathematically approximated by averaging all \(\beta\) values into a single \(\alpha\), which allows you to use the geometric shortcut formula \(\mathbb{E}[X] = \frac{1-\alpha^{\gamma+1}}{1-\alpha}\). While handy, it obscures the reality that token order matters!)</em></p> <hr/> <h2 id="part-4-the-objective-function-calculating-the-speedup">Part 4: The Objective Function (Calculating the Speedup)</h2> <p>We know how many tokens we get. Now, what is the wall-clock speedup compared to running the Target model alone?</p> <p>Let \(c \in [0,1]\) be the cost ratio: the time to draft one token divided by the time to verify one token.</p> <p><strong>1. The Cost of Speculative Decoding</strong> In units of the Target model’s time, one iteration requires:</p> <ul> <li>1 parallel target verification pass: Cost = \(1\)</li> <li>\(\gamma\) sequential draft passes: Cost = \(\gamma \cdot c\)</li> <li>Total Cost = \(\gamma c + 1\)</li> </ul> <p><strong>2. The Speedup Ratio</strong> If the Target model worked alone, producing \(\mathbb{E}[X]\) tokens would cost exactly \(\mathbb{E}[X]\) time units. By dividing the baseline time by our speculative time, we get the objective function:</p> \[\text{Speedup}(\gamma) = \frac{\mathbb{E}[X]}{\gamma c + 1}\] <p>Substituting our geometric approximation for \(\mathbb{E}[X]\) gives the standard theoretical formula:</p> \[\text{Speedup}(\gamma) = \frac{1 - \alpha^{\gamma+1}}{(1 - \alpha)(\gamma c + 1)}\] <h3 id="finding-the-sweet-spot">Finding the Sweet Spot</h3> <p>This formula perfectly illustrates the trade-off you must manage as an ML Engineer:</p> <ul> <li><strong>The Numerator (Tokens gained):</strong> Grows with \(\gamma\), but saturates. Because of the domino effect of rejections, drafting deeper and deeper yields rapidly diminishing returns.</li> <li><strong>The Denominator (Compute cost):</strong> Grows linearly. Every extra draft token costs real time, whether accepted or rejected.</li> </ul> <p>Because saturating gains are fighting linear costs, there is always an interior optimum \(\gamma^\star\). Drafting too far is actively wasteful. By understanding this math, you can dynamically tune your lookahead based on your Draft model’s alignment and your hardware constraints, squeezing maximum performance out of your inference infrastructure.</p>]]></content><author><name></name></author><category term="technical-writing"/><category term="llm-inference"/><category term="speculative-decoding"/><summary type="html"><![CDATA[A derivation of speculative decoding acceptance probability, expected accepted tokens, and the speedup objective.]]></summary></entry><entry><title type="html">The Math of AWQ: Protecting Salient Channels from the Inside Out</title><link href="https://sudhirpol522.github.io/blog/math-of-awq/" rel="alternate" type="text/html" title="The Math of AWQ: Protecting Salient Channels from the Inside Out"/><published>2026-06-15T00:00:00+00:00</published><updated>2026-06-15T00:00:00+00:00</updated><id>https://sudhirpol522.github.io/blog/math-of-awq</id><content type="html" xml:base="https://sudhirpol522.github.io/blog/math-of-awq/"><![CDATA[<p>When we compress a Large Language Model (LLM) from 16-bit floating-point numbers to 4-bit integers, we save a massive amount of memory. But compression creates mathematical damage. We are taking an infinite spectrum of continuous numbers and forcing them onto a rigid grid of integers.</p> <p>Activation-aware Weight Quantization (AWQ) is a brilliantly simple method to mitigate this damage. To understand why it works, we must build the math from the ground up.</p> <h2 id="step-1-the-anatomy-of-a-quantized-weight">Step 1: The Anatomy of a Quantized Weight</h2> <p>The core of quantization is mapping a floating-point weight (\(W\)) to an integer code, and then mapping it back. We define this entire round-trip process using a step size (\(\Delta\)):</p> \[Q(W) = \Delta \cdot \text{Round}\left(\frac{W}{\Delta}\right)\] <p>This elegant equation actually represents both halves of the process:</p> <ol> <li><strong>Quantization:</strong> We divide the weight by \(\Delta\) to find out how many “steps” it represents, and <code class="language-plaintext highlighter-rouge">Round()</code> it to snap it to the nearest whole integer.</li> <li><strong>Dequantization:</strong> We multiply that integer code back by \(\Delta\) to stretch it back into a floating-point approximation that the GPU can use for math.</li> </ol> <p>But how do we calculate \(\Delta\) in the first place?</p> <h2 id="step-2-defining-the-step-size-delta">Step 2: Defining the Step Size (\(\Delta\))</h2> <p>In <strong>symmetric quantization</strong>, we want to center our integer grid perfectly around zero.</p> <p>First, we look at our tensor of weights and find the largest absolute value. We call this maximum parameter \(a\):</p> \[a = \max(|W|)\] <p>This defines the floating-point range we need to capture: \([-a, +a]\). We must map this continuous interval onto our available discrete integer bins. For a bit-width of \(b\) (like INT4 or INT8), the signed integer range is:</p> \[[-(2^{b-1}-1), +(2^{b-1}-1)]\] <p>For INT4 (\(b=4\)), this gives us an integer range of \([-7, +7]\). <em>(Note: While standard INT4 can hold -8, we use a restricted range of -7 to +7 to keep the symmetry perfectly balanced around zero).</em></p> <p>Because we are mapping the total float distance (\(2a\)) to the total integer distance (\(14\)), the formula for our step size is:</p> \[\Delta = \frac{a - (-a)}{(2^{b-1}-1) - (-(2^{b-1}-1))} = \frac{2a}{2(2^{b-1}-1)} = \frac{a}{2^{b-1}-1}\] <blockquote> <p><strong>The Intuition Example:</strong> Imagine a group of weights where the maximum absolute value is <strong>10</strong> (\(a = 10\)). For our INT4 grid, \(\Delta = 10 / 7 \approx\) <strong>1.43</strong>. \(\Delta\) is the slope of the line connecting our codes to reality. It means that every time our integer code moves by <strong>1</strong>, the real floating-point value jumps by <strong>1.43</strong>.</p> </blockquote> <h2 id="step-3-deriving-the-output-error">Step 3: Deriving the Output Error</h2> <p>A neural network layer works by multiplying an input activation (\(x\)) by a weight (\(W\)). The perfect output is \(Wx\). The compressed output is \(Q(W)x\).</p> <p>To see the damage caused by compression, we calculate the Output Error (\(\text{Err}\)):</p> \[\text{Err}(Q(W)x) = Q(W)x - Wx\] \[\text{Err}(Q(W)x) = (Q(W) - W)x\] <p>Let’s look at \((Q(W) - W)\). What is the difference between a quantized weight and a true weight? It is simply the fractional rounding error multiplied by our step size! We can formalize this as:</p> \[(Q(W) - W) = \Delta \cdot \text{RoundErr}\left(\frac{W}{\Delta}\right)\] <p>Substitute this back into our equation, and we arrive at the foundational formula of the AWQ paper:</p> \[\text{Err}(Q(W)x) = \Delta \cdot \text{RoundErr}\left(\frac{W}{\Delta}\right) \cdot x\] <h2 id="step-4-the-statistical-reality-of-rounding">Step 4: The Statistical Reality of Rounding</h2> <p>Look closely at the error equation. It has three parts: \(\Delta\), the Rounding Error, and the activation \(x\).</p> <p>The paper makes a crucial statistical observation about the <code class="language-plaintext highlighter-rouge">RoundErr</code> term. When you divide thousands of random weights by \(\Delta\), their fractional parts are spread evenly between 0 and 1. Therefore, when you round them to the nearest integer, the errors are uniformly distributed between -0.5 and +0.5.</p> <p>If we only care about the magnitude of the error, <strong>the average absolute rounding error is always roughly 0.25 steps.</strong> In modern hardware, we use <strong>group-wise quantization</strong>, meaning 128 weights are forced to share the exact same \(\Delta\).</p> <ul> <li>If \(\Delta\) is fixed for the group…</li> <li>And <code class="language-plaintext highlighter-rouge">RoundErr</code> averages out to a fixed <strong>0.25</strong>…</li> </ul> <p><strong>Then the quantization error in the output is strictly proportional to the input activation (\(x\)).</strong></p> <p>If the activation flowing into a weight is small, the error is tiny. But if the network relies on a massive, “salient” activation channel, that large \(x\) multiplies with the grid misalignment and produces a catastrophic discrepancy in the final output.</p> <h2 id="step-5-the-awq-solution-scaling-down-activations">Step 5: The AWQ Solution: Scaling Down Activations</h2> <p>We are trapped by algebra. We cannot shrink \(\Delta\) or stop the rounding error. To save the network, we must mathematically reduce the input activation magnitude (\(x\)).</p> <p>We do this by introducing a scaling factor, \(s\). We scale down the dangerous activation by dividing it:</p> \[\text{New Activation} = \frac{x}{s}\] <p>But to keep the layer’s overall computation mathematically identical (re-parameterization), we must multiply the weight by that exact same \(s\):</p> \[\text{New Weight} = W \cdot s\] <p>In perfect floating-point math, \((W \cdot s)(x / s) = Wx\). The network’s intelligence remains untouched. But the magic happens during quantised inference, because the rounding now acts on the <em>scaled</em> weights.</p> <h2 id="step-6-the-delta-dash-trap-and-the-tug-of-war">Step 6: The “Delta Dash” Trap and the Tug of War</h2> <p>If dividing the activation by \(s\) is good, why not use a massive scale like \(s=100\) and shrink the error to zero?</p> <p>Because of the group \(\Delta\). Remember, \(\Delta\) is defined by the maximum weight in the group. If we take a salient weight (\(W\)) and multiply it by a scale \(s_i &gt; 1\), that weight physically grows. If we scale it so much that it becomes the <em>new</em> maximum for the group, it inflates the step size.</p> <p>The paper defines this new inflated step size as \(\Delta'\) (Delta Dash). Our output error equation transforms into a brutal tug of war:</p> \[\text{Err}\left(Q(Ws)\left(\frac{x}{s}\right)\right) = \Delta' \cdot \text{RoundErr}\left(\frac{Ws}{\Delta'}\right) \cdot \left(\frac{x}{s}\right)\] <blockquote> <p><strong>The Trade-off in Action:</strong></p> <ul> <li><strong>The Good:</strong> By shrinking the activation multiplier to \((x/s)\), we drastically reduce the effective contribution of quantization error from the channels where it matters most.</li> <li><strong>The Bad:</strong> If \(s\) is too large, the scaled weight \(Ws\) pushes \(\Delta'\) higher (\(\Delta' &gt; \Delta\)). A wider step size ruins the grid precision for <em>every other normal weight</em> in that group.</li> </ul> </blockquote> <p>We must find the perfect scale \(s\) that shrinks the activation, but stops <em>just</em> before it inflates \(\Delta'\).</p> <h2 id="step-7-optimizing-the-scaling-factors-alpha">Step 7: Optimizing the Scaling Factors (\(\alpha\))</h2> <p>How do we find this perfect \(s\)? In traditional deep learning, we would train the neural network using gradients and backpropagation.</p> <p>But AWQ avoids this. Because the error relies on a <code class="language-plaintext highlighter-rouge">Round()</code> function, which looks like a flat staircase on a graph, its mathematical derivative is exactly zero. Gradients cannot flow through step functions reliably, making gradient-based optimization wildly unstable and incredibly memory-hungry.</p> <p>Instead, the authors use pure logic. We know a weight channel only needs to be scaled up if the activations flowing through it are large. Therefore, the scale \(s\) should just be a mathematical reflection of the average activation magnitude (\(s_X\)).</p> <p>They define the scale using a single dial, \(\alpha\):</p> \[s = s_X^\alpha\] <p>Rather than training, they perform a lightning-fast <strong>Grid Search</strong> over \(\alpha \in [0, 1]\):</p> <ul> <li><strong>When \(\alpha = 0\):</strong> There is effectively no scaling (\(s=1\)). We get naive quantization, and loud activations destroy the output.</li> <li><strong>When \(\alpha = 1\):</strong> We get the most aggressive scaling possible (\(s=s_X\)). We fully protect the salient channel, but we risk inflating \(\Delta'\) and ruining the rest of the group.</li> </ul> <p>By testing a few points between 0 and 1, AWQ elegantly finds the \(\alpha\) that optimally balances the tug of war: scaling salient channels up just enough to land on cleaner grid points, shrinking the dangerous activations, and keeping the group’s \(\Delta\) safe.</p> <hr/> <h2 id="references">References</h2> <ul> <li>Hamza Elshafie, <a href="https://hamzaelshafie.bearblog.dev/awq-activation-aware-weight-quantisation/">AWQ: Activation-aware Weight Quantisation</a></li> <li>Lin et al., <a href="https://arxiv.org/abs/2306.00978">AWQ: Activation-aware Weight Quantization for LLM Compression and Acceleration</a> (original paper)</li> </ul>]]></content><author><name></name></author><category term="technical-writing"/><category term="llm-inference"/><category term="quantization"/><category term="awq"/><summary type="html"><![CDATA[A first principles explanation of activation aware weight quantization and its scaling tradeoffs.]]></summary></entry><entry><title type="html">Demystifying GPTQ: From Lagrange Multipliers to Vectorized PyTorch</title><link href="https://sudhirpol522.github.io/blog/demystifying-gptq/" rel="alternate" type="text/html" title="Demystifying GPTQ: From Lagrange Multipliers to Vectorized PyTorch"/><published>2026-06-14T00:00:00+00:00</published><updated>2026-06-14T00:00:00+00:00</updated><id>https://sudhirpol522.github.io/blog/demystifying-gptq</id><content type="html" xml:base="https://sudhirpol522.github.io/blog/demystifying-gptq/"><![CDATA[<p>If you are optimizing inference engines or writing custom CUDA kernels for modern LLMs, you inevitably hit the memory wall. Serving a 70B parameter model in FP16 requires over 140GB of VRAM, forcing you into multi-GPU tensor parallelism just to load the weights.</p> <p>Post-Training Quantization (PTQ) algorithms like GPTQ solve this by compressing weights down to 4-bits or 3-bits. But if you naively round the weights of a massive LLM, you destroy the model’s perplexity.</p> <p>GPTQ survives aggressive quantization by recognizing a fundamental truth: <strong>weights do not act in isolation</strong>. If you force one weight down, you can calculate exactly how much to push the neighboring weights up to absorb the structural damage.</p> <p>In this post, we will derive the exact mathematics behind GPTQ from scratch using Lagrange multipliers, shadow-run a 2D example, and finally translate the derivation into highly optimized, vectorized PyTorch code.</p> <hr/> <h2 id="part-1-the-problem-formulation">Part 1: The Problem Formulation</h2> <p>Imagine a single linear layer. We have an input matrix \(X\) and a weight matrix \(W\). We want to quantize a specific weight \(w_q\) to a target integer value, \(quant(w_q)\).</p> <p>This introduces a forced change, or a quantization error, which we will call \(\Delta_q\):</p> \[\Delta_q = quant(w_q) - w_q\] <p>Our goal is to find the optimal vector of weight updates, \(\delta\), for the entire row of weights to compensate for this forced change. We want to minimize the mean squared error (MSE) of the layer’s output.</p> <p>Using a second-order Taylor expansion, the error introduced by changing the weights by \(\delta\) is approximated by the Hessian matrix \(H\):</p> \[E(\delta) = \frac{1}{2} \delta^T H \delta\] <p>Where \(H = 2 X X^T\) (the uncentered covariance of the input features).</p> <p><strong>The Constraint:</strong> We are not minimizing this error freely. We are locked to a strict rule: the \(q\)-th element of our update vector \(\delta\) <em>must</em> equal our forced quantization change \(\Delta_q\). Let \(\mathbf{u}_q\) be a unit vector (all zeros with a \(1\) at index \(q\)). Our constraint is:</p> \[\mathbf{u}_q^T \delta = \Delta_q\] <hr/> <h2 id="part-2-the-derivation-lagrange-multipliers">Part 2: The Derivation (Lagrange Multipliers)</h2> <p>We need to minimize a quadratic equation subject to a linear constraint. This is the textbook definition of a Lagrange multiplier problem.</p> <h3 id="step-1-write-the-lagrangian">Step 1: Write the Lagrangian</h3> <p>We combine our objective and our constraint using a dummy variable, \(\lambda\):</p> \[L(\delta, \lambda) = \frac{1}{2} \delta^T H \delta + \lambda (\mathbf{u}_q^T \delta - \Delta_q)\] <h3 id="step-2-set-the-gradient-to-zero">Step 2: Set the gradient to zero</h3> <p>To find the bottom of the error curve, we take the derivative with respect to the vector \(\delta\) and set it to zero:</p> \[\frac{\partial L}{\partial \delta} = H \delta + \lambda \mathbf{u}_q = 0\] <p>Solving for the optimal update direction \(\delta\):</p> \[H \delta = -\lambda \mathbf{u}_q\] \[\delta = -\lambda H^{-1} \mathbf{u}_q \quad \text{... (Equation i)}\] <blockquote> <p><strong>The Insight:</strong> Multiplying any matrix by a unit vector \(\mathbf{u}_q\) extracts exactly the \(q\)-th column. This proves that the optimal way to update <em>all</em> the weights when you quantize weight \(q\) is to move them in the exact proportions dictated by the \(q\)-th column of the inverse Hessian.</p> </blockquote> <h3 id="step-3-solve-for-lambda">Step 3: Solve for \(\lambda\)</h3> <p>We know our direction, but we need the exact scale. We substitute Equation (i) back into our strict constraint:</p> \[\mathbf{u}_q^T (-\lambda H^{-1} \mathbf{u}_q) = \Delta_q\] \[-\lambda (\mathbf{u}_q^T H^{-1} \mathbf{u}_q) = \Delta_q\] <p>The term \((\mathbf{u}_q^T H^{-1} \mathbf{u}_q)\) extracts exactly the diagonal entry of the inverse Hessian at index \((q, q)\).</p> \[-\lambda [H^{-1}]_{qq} = \Delta_q\] \[\lambda = \frac{-\Delta_q}{[H^{-1}]_{qq}}\] <h3 id="step-4-the-final-update-formula">Step 4: The Final Update Formula</h3> <p>Substitute \(\lambda\) back into Equation (i) to get the exact, final update vector:</p> \[\delta = \left( \frac{\Delta_q}{[H^{-1}]_{qq}} \right) \cdot H^{-1}[:, q]\] <hr/> <h2 id="part-3-a-shadow-run-with-real-numbers">Part 3: A Shadow Run with Real Numbers</h2> <p>Let’s ground this math. Suppose we have an inverse Hessian for a 2-feature input, and we are updating a single output neuron with 2 weights: \(w = [0.8, 0.6]^T\).</p> \[H^{-1} = \begin{bmatrix} 1 &amp; -1 \\ -1 &amp; 2 \end{bmatrix}\] <p>We want to quantize \(w_1\) (currently <strong>0.8</strong>) to <strong>1.0</strong>.</p> <ul> <li>Our forced change is \(\Delta_1 = 0.2\).</li> <li>We look at the first column of \(H^{-1}\): \([1, -1]^T\).</li> <li>Our diagonal \([H^{-1}]_{11}\) is \(1\).</li> </ul> <p><strong>Calculate the update vector \(\delta\):</strong></p> \[\delta = \left( \frac{0.2}{1} \right) \cdot \begin{bmatrix} 1 \\ -1 \end{bmatrix} = \begin{bmatrix} 0.2 \\ -0.2 \end{bmatrix}\] <p><strong>The Result:</strong> Weight 1 shifts by \(+0.2\) (landing perfectly on our quantized integer \(1.0\)). Because the input data is correlated, forcing weight 1 up damages the network. To perfectly absorb that structural damage, the Hessian tells us Weight 2 must shift by \(-0.2\). The new unquantized weight becomes \(0.4\).</p> <hr/> <h2 id="part-4-the-secret-of-row-independence">Part 4: The Secret of Row Independence</h2> <p>If we have a massive weight matrix of shape <code class="language-plaintext highlighter-rouge">[out_features, in_features]</code>, do we need a massive, uncomputable Hessian?</p> <p>No. In a linear layer, <strong>output neurons do not share weights</strong>. When you change a weight in Row 1, you only damage the output of Neuron 1. Neuron 2 is completely unaffected.</p> <p>Therefore, we only need the Hessian of the <em>input features</em> (shape <code class="language-plaintext highlighter-rouge">[in_features, in_features]</code>). We can apply this exact same small inverse Hessian to optimize every single row of the weight matrix independently.</p> <p>Even better, we don’t have to write a <code class="language-plaintext highlighter-rouge">for</code> loop across the rows. We can use vectorization to update every single output neuron simultaneously.</p> <hr/> <h2 id="part-5-the-vectorized-pytorch-implementation">Part 5: The Vectorized PyTorch Implementation</h2> <p>Here is the exact mathematical core of GPTQ translated into a high-throughput PyTorch execution.</p> <p>Notice how the algorithm marches sequentially through the columns (\(q = 0, 1, 2...\)). Once a column is quantized, it is locked. The compensation update is <em>only</em> applied to the remaining columns to its right (<code class="language-plaintext highlighter-rouge">q+1:</code>). If we applied the update to the whole matrix, we would add floating-point noise back onto our already-quantized integers, ruining our previous work.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">torch</span>

<span class="k">def</span> <span class="nf">quantize_to_nearest</span><span class="p">(</span><span class="n">w</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    Placeholder for symmetric/affine quantization.
    </span><span class="sh">"""</span>
    <span class="k">return</span> <span class="n">torch</span><span class="p">.</span><span class="nf">round</span><span class="p">(</span><span class="n">w</span><span class="p">)</span>

<span class="k">def</span> <span class="nf">gptq_core</span><span class="p">(</span><span class="n">W</span><span class="p">,</span> <span class="n">H</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    W: Weight matrix of shape [out_features, in_features]
    H: Hessian matrix of shape [in_features, in_features]
    </span><span class="sh">"""</span>
    <span class="n">out_features</span><span class="p">,</span> <span class="n">in_features</span> <span class="o">=</span> <span class="n">W</span><span class="p">.</span><span class="n">shape</span>
    <span class="n">Q</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros_like</span><span class="p">(</span><span class="n">W</span><span class="p">)</span>

    <span class="c1"># 1. Hessian Preparation (Dampening for numerical stability)
</span>    <span class="n">dead</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">diag</span><span class="p">(</span><span class="n">H</span><span class="p">)</span> <span class="o">==</span> <span class="mi">0</span>
    <span class="n">H</span><span class="p">[</span><span class="n">dead</span><span class="p">,</span> <span class="n">dead</span><span class="p">]</span> <span class="o">=</span> <span class="mi">1</span>
    <span class="n">dampening</span> <span class="o">=</span> <span class="mf">0.01</span> <span class="o">*</span> <span class="n">torch</span><span class="p">.</span><span class="nf">mean</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">diag</span><span class="p">(</span><span class="n">H</span><span class="p">))</span>
    <span class="n">H</span> <span class="o">=</span> <span class="n">H</span> <span class="o">+</span> <span class="n">torch</span><span class="p">.</span><span class="nf">eye</span><span class="p">(</span><span class="n">in_features</span><span class="p">)</span> <span class="o">*</span> <span class="n">dampening</span>

    <span class="c1"># In production, use Cholesky decomposition instead of raw inverse
</span>    <span class="c1"># to prevent floating-point catastrophic cancellation on massive matrices.
</span>    <span class="c1"># H_inv = torch.linalg.cholesky(torch.linalg.inv(H), upper=True)
</span>    <span class="n">H_inv</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">linalg</span><span class="p">.</span><span class="nf">inv</span><span class="p">(</span><span class="n">H</span><span class="p">)</span>

    <span class="c1"># 2. The Core Quantization Loop (Marching left to right)
</span>    <span class="k">for</span> <span class="n">q</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">in_features</span><span class="p">):</span>

        <span class="c1"># Grab the q-th column (active weight for all output neurons)
</span>        <span class="n">w_q</span> <span class="o">=</span> <span class="n">W</span><span class="p">[:,</span> <span class="n">q</span><span class="p">]</span>

        <span class="c1"># Quantize and lock into our final output matrix
</span>        <span class="n">quantized_w_q</span> <span class="o">=</span> <span class="nf">quantize_to_nearest</span><span class="p">(</span><span class="n">w_q</span><span class="p">)</span>
        <span class="n">Q</span><span class="p">[:,</span> <span class="n">q</span><span class="p">]</span> <span class="o">=</span> <span class="n">quantized_w_q</span>

        <span class="c1"># Step 1: Calculate forced change (shape: [out_features])
</span>        <span class="n">delta_q</span> <span class="o">=</span> <span class="n">quantized_w_q</span> <span class="o">-</span> <span class="n">w_q</span>

        <span class="c1"># Step 3: Calculate scale factor lambda (shape: [out_features])
</span>        <span class="n">scale_factor</span> <span class="o">=</span> <span class="n">delta_q</span> <span class="o">/</span> <span class="n">H_inv</span><span class="p">[</span><span class="n">q</span><span class="p">,</span> <span class="n">q</span><span class="p">]</span>

        <span class="c1"># Step 4: The Vectorized Compensation
</span>        <span class="c1"># We grab the remaining slice of the inverse Hessian.
</span>        <span class="c1"># Note: We slice the row (H_inv[q, q+1:]) instead of the column
</span>        <span class="c1"># because PyTorch memory is C-contiguous (row-major). Since the
</span>        <span class="c1"># Hessian is perfectly symmetric, row == col, but row is vastly
</span>        <span class="c1"># faster for the GPU memory bus.
</span>        <span class="n">h_inv_slice</span> <span class="o">=</span> <span class="n">H_inv</span><span class="p">[</span><span class="n">q</span><span class="p">,</span> <span class="n">q</span><span class="o">+</span><span class="mi">1</span><span class="p">:]</span>

        <span class="c1"># torch.outer computes the update for ALL output neurons instantly
</span>        <span class="n">update</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">outer</span><span class="p">(</span><span class="n">scale_factor</span><span class="p">,</span> <span class="n">h_inv_slice</span><span class="p">)</span>

        <span class="c1"># Apply the compensation ONLY to the remaining unquantized weights
</span>        <span class="n">W</span><span class="p">[:,</span> <span class="n">q</span><span class="o">+</span><span class="mi">1</span><span class="p">:]</span> <span class="o">=</span> <span class="n">W</span><span class="p">[:,</span> <span class="n">q</span><span class="o">+</span><span class="mi">1</span><span class="p">:]</span> <span class="o">+</span> <span class="n">update</span>

    <span class="k">return</span> <span class="n">Q</span>
</code></pre></div></div> <h3 id="engineering-polish-why-cholesky">Engineering Polish: Why Cholesky?</h3> <p>If you run the standard <code class="language-plaintext highlighter-rouge">torch.linalg.inv</code> on a 70B model, the loop will fail. The inverse of million-element matrices introduces tiny floating-point rounding errors. By the time the loop reaches the final columns, those errors compound exponentially, and the weight updates become catastrophic.</p> <p>Because the Hessian matrix is perfectly symmetrical and positive-definite, production quantization engines swap the raw inverse for a <strong>Cholesky decomposition</strong> of the inverse (<code class="language-plaintext highlighter-rouge">torch.linalg.cholesky</code>). This heavily optimized, triangular matrix perfectly handles floating-point stabilization, allowing the exact same loop to compress a massive LLM on a single GPU in hours.</p> <hr/> <h3 id="conclusion">Conclusion</h3> <p>Algorithms like GPTQ and AWQ are what allow modern serving infrastructure to exist at scale. By understanding the underlying continuous mathematics, Lagrange multipliers, and second-order Hessian approximations, we can write the discrete, vectorized matrix operations required to optimize the next generation of inference engines.</p>]]></content><author><name></name></author><category term="technical-writing"/><category term="llm-inference"/><category term="quantization"/><category term="gptq"/><summary type="html"><![CDATA[A derivation of the inverse Hessian update used by GPTQ, followed by a vectorized PyTorch implementation.]]></summary></entry></feed>